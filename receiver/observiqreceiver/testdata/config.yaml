receivers:
  # TODO fully default config
  # observiq:
  observiq/generate:
    plugins_dir: local/plugins
    pipeline:
      - type: sample_plugin
        message: hello
      - type: receiver_output

# processors:
#   exampleprocessor:

exporters:
  file:
    path: local/exporter.log

service:
  pipelines:
    logs:
      receivers: [observiq/generate]
      # processors: [exampleprocessor]
      exporters: [file]
