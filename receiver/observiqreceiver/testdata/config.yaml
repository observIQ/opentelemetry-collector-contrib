receivers:
  observiq/generate:
    plugins_dir: local/plugins
    pipeline:
      - type: sample_plugin
        message: hello
      - type: receiver_output

exporters:
  file:
    path: local/exporter.log

service:
  pipelines:
    logs:
      receivers: [observiq/generate]
      exporters: [file]
