receivers:
  observiq/generate:
    pipeline:
      - type: generate_input
        entry:
          severity: 70
          record: log1
      - type: rate_limit
        rate: 10
      - type: channel_output

# processors:
#   exampleprocessor:

exporters:
  file:
    path: local/exporter.out

service:
  pipelines:
    logs:
      receivers: [observiq/generate]
      # processors: [exampleprocessor]
      exporters: [file]
