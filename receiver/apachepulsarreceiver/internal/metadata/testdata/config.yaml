default:
all_set:
  metrics:
    pulsar.topic.avgmsgsize:
      enabled: true
    pulsar.topic.msginrate:
      enabled: true
    pulsar.topic.sub.unackedmsgs:
      enabled: true
  resource_attributes:
none_set:
  metrics:
    pulsar.topic.avgmsgsize:
      enabled: false
    pulsar.topic.msginrate:
      enabled: false
    pulsar.topic.sub.unackedmsgs:
      enabled: false
  resource_attributes:
