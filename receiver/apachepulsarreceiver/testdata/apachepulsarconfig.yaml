# ./bin/otelcontribcol_darwin_arm64 --config ./receiver/apachepulsarreceiver/testdata/apachepulsarconfig.yaml
receivers:
  apachepulsar:
    collection_interval: 60s
exporters:
  logging:
    loglevel: debug

service:
  pipelines:
    metrics:
      receivers: [apachepulsar]
      exporters: [logging]
