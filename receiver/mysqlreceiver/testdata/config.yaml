receivers:
  mysql:
    collection_interval: 10s
    user: otel
    password: otel
    address: 127.0.0.1
    port: 3306

processors:


exporters:
  logging:   
    loglevel: debug

service:
  pipelines:
    metrics:
     receivers: [mysql]
     processors: []
     exporters: [logging]
