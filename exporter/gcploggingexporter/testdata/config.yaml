receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  gcplogging:
    project: my-gcp-project
    credentials_file: /path/to/creds/with/logging.write

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [gcplogging]
