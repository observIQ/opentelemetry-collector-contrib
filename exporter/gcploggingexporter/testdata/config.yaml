receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  gcplogging:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [gcplogging]
