service:
  telemetry:
    metrics:
      address: ""
      readers:
        - periodic:
            exporter:
              otlp:
                endpoint: "{{ .MetricsEndpoint }}"
                protocol: "http/protobuf"
            interval: 10000 # in ms
